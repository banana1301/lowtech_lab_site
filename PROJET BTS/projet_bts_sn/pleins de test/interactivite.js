

function Recuperation_value() {
document.getElementById("menu1").value = option ;
    if (option === "1") {

        pass;
    }
    else if (option === "2") {

        pass;
    }
    else if (option === "3") {

        pass;
    }
    else if (option === "4") {

        pass;
    }
    else if (option === "5") {

        pass;
    }
    else if (option === "6") {

        pass;
    }
    else if (option === "7") {

        pass;
    }
    else if (option === "8") {

        pass;
    }
    else if (option === "0"){

        pass;
    }
}
